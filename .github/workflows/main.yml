name: Testing Action Run

on:
  push:
  workflow_dispatch:


jobs: 
  testing:
    runs-on: ubuntu-latest
    environment:
      name: um_nome_qualquer
    steps:
      - name: Checkout
        id: teste
        uses: actions/checkout@v4

      - run: echo ${{ jobs }}
        shell: bash